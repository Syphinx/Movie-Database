import { join } from "path";

export const PAGES_PATH = join(__dirname, "views", "pages");

export const PUBLIC_FOLDER = join(__dirname, "..", "public");

export const MONGO_URI = "mongodb://127.0.0.1:27017/project2";

export const PORT = 3001;
